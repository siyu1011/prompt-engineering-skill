# Prompts

This directory contains platform-neutral prompt assets.

Use these files directly in any AI product:

- `general/`: generic prompt templates
- `chinese/`: Chinese-language prompt templates
- `system/`: system prompt and agent prompt templates
- `scenarios/`: templates grouped by work context
- `reviews/`: prompt review and audit templates
